# 2026-09-02 工作日志

## 项目：HZT 每日股市简报

### 1. 输出模板改回"旧格式"（自包含内联）
- 改 `stock_report.py`：`generate_html_report()` 增加 `_load_asset()` 辅助，把 `docs/style.css` / `docs/script.js` 内联进每个 HTML，不再外链 `style.css?v=`。
- 背景：历史 188 份报告中 134 份内联、54 份外链，且 style.css 被改过 3 版，导致"格式天天不一样"。本次只改生成器，不动历史报告。
- 容错：读不到 css/js 时回退外链，生成不中断。

### 2. 提示词加固：禁止 LLM 输出思考过程/内心独白
- 问题现象：UP主观点输出出现"好的，用户给了一段很长的文本……"及"按照用户给的格式…我理解为…"两类思维链泄漏。
- 根因：代码 `_call_deepseek()` 取 `content`（第413行），模型把 CoT 写进 content；非代码 bug，纯提示词缺失约束。
- 修复：给全部 8 个 `prompts/*.txt` 加"输出纪律/硬性要求"硬约束（4 个 system 末尾 + 4 个 user 开头）。

### 2b. UP主观点提示词升级为"投资级" schema（第二轮）
- 用户反馈实际输出仍混乱：格式协商内心戏泄漏 + "情绪倾向"因旧提示词"乐观/中性/悲观（标为利好/中性/利空）"歧义，模型输出"中性偏乐观（利好）"。
- 修复 `system_opinion.txt`：输出纪律追加禁用"格式协商"类表述（按照用户给的格式/我理解为/用户要求/其实对于），明确"像机构晨会纪要"。
- 重写 `user_opinion.txt`：
  - 硬性要求明确"情绪倾向只能利好/中性/利空三选一，禁'偏X'模糊表述"。
  - 输出 schema 字段化（核心观点/情绪倾向/看多方向/看空谨慎方向/关键论据/置信度），去掉原"核心逻辑"冗余标题。
  - 共识与分歧表格列改为 | 板块/方向 | 看多UP数 | 看空UP数 | 代表UP主 | 逻辑 |，"看多提示减仓/看空提示抄底"写入逻辑列。
  - 新增 few-shot 输出示例锚定格式。
- 生成 `preview_opinion_20260902.html` 预览投资级渲染效果。

### 2c. 其余三块提示词统一为投资级格式（第三轮，用户说"继续修改"）
- 目标：大盘分析 / 选股 / 信息差 与 UP主观点保持四块风格统一。
- 3 个 `system_*.txt`（analyst/stock_picker/info_gap）：输出纪律补齐格式协商禁令 + "机构晨会纪要"口径（与 system_opinion 一致）。
- 3 个 `user_*.txt` 重写：
  - user_analyst：结构改 ## 分节（资金面/主线方向/跨市场/要闻速览表格/市场体温五选一/明日情景），新增 few-shot；市场体温禁自创词。
  - user_stock_picker：输出格式改为 ### 主题 + 表格 | 股票及代码 | 选股逻辑 | 方向 | 把握度 |，新增 few-shot。
  - user_info_gap：字段化（核心动态/行业板块关注/个股公司动态/数据指标变动），新增 few-shot。
- 校验：8 文件均含格式协商禁令；4 user 文件含 few-shot；4 system 含机构晨会纪要；5 个数据占位符(opinion_text/news_text/opinion_context/info_context/info_text)均保留在正确文件。

### 3. 项目结构结论（用户询问）
- 被每日构建反复生成的：docs/report_*.html、docs/index.html、docs/archive.html、docs/charts/*.png、docs/pdf/*.pdf、report_*.md（后两者被 .gitignore 排除，仅作 Artifact）。
- 与自动更新无关、不重新生成的：6 个 .py 源码、prompts/8个txt、docs/style.css+script.js+.nojekyll、up主的每日观点/*、Dockerfile/compose/requirements/workflow_payload.json、.github/workflows/*.yml、SETUP_GUIDE.md、.kimi 技能、.gitignore、__pycache__。

### 4. 前端主题改造：学术期刊级（去装饰化·数据优先·极度克制）
- 用户给定完整设计 spec（顶级 frontend / 学术期刊图表专家角色定位 + 审美规则 + 元素映射 + 负面清单 + 交付前检查）。
- 改 `docs/style.css`（整体重写）：衬线体(Times New Roman/Source Serif 4/Noto Serif SC/宋体)替代原 Inter 无衬线；纯白 #FFF 背景；低饱和学术配色(藏青#1f3a5f/砖红#9c3b3b→涨/灰绿#3f6b4f→跌/灰蓝#5b7a99)；发丝边框；半去框化；booktabs 表格(仅水平线)；扁平单色条形图；无阴影无圆角无渐变。
- 改 `stock_report.py`：资金流条形图内联颜色改为砖红(流入)/灰绿(流出)，修正原"绿=流入/红=流出"与中国涨红跌绿习惯相反；K线说明文字改砖红；3 处 sec-hdr 改"主标题+副标题"双行(中文主标题+英文/数据源副标题，匹配 CSS .sec-main/.sec-sub)。
- 改 `stock_analyzer.py`：matplotlib K线图学术化——setup_matplotlib 改优先衬线中文字体(宋体/思源宋体，非雅黑黑体)；rcParams 设白底、去上右边框、虚线浅灰#E0E0E0网格、低饱和配色；generate_chart 改双行标题(主结论+括号数据源)、K线砖红/灰绿、MA 用实/虚/点线+灰阶区分、x标签不倾斜、图例无框置于内部。
- 注：本地 venv 装 matplotlib 受沙箱网络限制失败(killed)，用 Python 直接生成 `preview_chart_sample.svg` 作为学术风样例图(纯白/衬线/砖红灰绿/虚线网格/内部无框图例/双行标题)，requirements.txt 已含 matplotlib>=3.5 真管线可用。
- 交付前检查：CSS 已含 @media(max-width:680px/640px) 移动端适配(图表单列、缩小字号、行情条横向滚动、收藏面板移动端隐藏防遮挡)；section 标题/表头无文字被图遮挡(图 max-width 620px 居中、height:auto)。
- 预览：`preview_full_20260902.html`(引用 SVG 的完整四块报告) + `preview_chart_sample.svg`(样例K线)。
