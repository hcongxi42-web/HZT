# 2026-09-03

## 网站体验审查改进（保持学术风，翻译审查建议）
用户贴了一份第一性原理+对抗性审查，我筛出可采纳项并确认方向：保持 9/2 锁定的学术期刊风，把审查的 UX 建议翻译成学术语言。

落地改动（均未推 GitHub，CI 仍跑旧版）：
- `stock_report.py`：
  - 新增 `_split_lead`/`_render_lead_box`：从报告抽 `## 今日要点` 渲染为顶部学术风摘要框（市场体温/共识/分歧/风险/策略）。
  - `markdown_to_html` 重构：任意含「情绪倾向」的 `###` 小节自动渲染 UP主情绪(涨红跌绿)/风格 chip + 一句话操作；支持 `>` 引用；`.sec` 加真实 `id` 锚点。
  - `generate_html_report`：加 TOC 目录锚点导航（平滑滚动）、报头来源行、回到顶部按钮、`up_count` 参数；`format_stock_picks` 前置 AI选股方法论+免责声明。
- `docs/style.css`：新增 `.toc/.lead-box/.op-chip/.op-action/.note/.to-top` 样式；`html` 平滑滚动+锚点偏移；窄屏目录横滑/要点行堆叠/表格卡片化(≤520px)。
- `docs/script.js`：表格 `data-th` 注入(移动端卡片标签) + 回到顶部显隐。
- 提示词：`user_analyst.txt` 要求首段 `## 今日要点` + 明天怎么看标概率；`user_opinion.txt` 加「风格」「一句话操作」字段。
- 本地 mock 渲染验证通过（preview_review_20260903.html），无 API 调用。

待办：用可连 GitHub 的代理 `git push`，让 CI 跑新代码；审查中暂缓项（UP主胜率/历史验证/字体切换/术语悬浮）未做。

## 报头数据来源标注修正（用户编辑 HTML → 同步回代码）
用户编辑 preview_review_20260903.html 的报头，把模型名从「数据来源」中剥离、单列署名行。已同步回代码：
- `stock_report.py` L1145：原 `数据来源：东方财富 · 新浪财经 · DeepSeek V4{up_src}` → 拆成两行 `数据来源：东方财富 · 新浪财经{up_src}` + `作者＆模型：HZT ＆ Deepseek V4 Flash`（模型非数据源，纠正归类；`{up_src}`=「· 综合 N 位 UP 主观点」仍属数据源保留）。
- `docs/style.css`：新增 `.masthead-src/.masthead-by` 基础样式，移动端覆盖规则同步生效。
- 验证：up_count=0 与 =3 两种渲染报头均正确；模型名不再出现在数据来源行。
- 未动 footer 的 `AI · DeepSeek V4`（L1174，页脚署名，非本次编辑范围）。
